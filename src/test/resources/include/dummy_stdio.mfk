noinline void putchar(byte b) { }
noinline void putstrz(pointer p) { putchar(0) }
byte strzlen(pointer str) {
    byte index
    index = 0
    while str[index] != 0 {
        index += 1
    }
    return index
}
