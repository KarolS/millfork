import test_fibonacci
import test_pstring
import test_string

void main() {
    ensure_mixedcase()

    test_fibonacci()
    test_string()
    test_pstring()

    new_line()
    putstrz("Total failures: "z)
    putword(failure_count)
}