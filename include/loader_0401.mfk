#if not(CBM)
#warn loader_0401 module should be only used on Commodore targets
#endif

const array _basic_loader @$401 = [
    $0b,
    4,
    10,
    0,
    $9e,
    $31,
    $30,
    $33,
    $37,
    0,
    0,
    0
    ]

